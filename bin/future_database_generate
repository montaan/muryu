#!/usr/bin/ruby
require 'future/database/creator'
require 'pathname'

db = ARGV[0] || '.'
if File.directory?(db)
  files = Dir[File.join(db, '*.rb')]
else
  files = [db]
end

puts DB::Creator.new(db)

